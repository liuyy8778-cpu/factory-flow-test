import {Input} from '@/components/ui/input';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
import {blankEnd,type EndCut} from './drawing-types';
export default function EndInline({value:e,onChange,label}:{value:EndCut;onChange:(e:EndCut)=>void;label:string}){return <div className="end-inline"><Select value={e.mode} onValueChange={v=>onChange({...blankEnd(),mode:v as EndCut['mode']})}><SelectTrigger aria-label={label+'加工狀態'}><SelectValue/></SelectTrigger><SelectContent><SelectItem value="pending">待確認</SelectItem><SelectItem value="machine">車修外徑</SelectItem><SelectItem value="unchanged">外徑不加工</SelectItem></SelectContent></Select>{e.mode==='machine'&&<div className="form-grid">{[['diameter','外徑'],['cut_length','加工長度'],['diameter_tolerance','外徑公差'],['cut_length_tolerance','長度公差']].map(([k,t])=><label className="field" key={k}><span>{t}</span><Input aria-label={label+t} placeholder="待填" inputMode={k.includes('tolerance')?'text':'decimal'} value={e[k as keyof EndCut]} onChange={ev=>onChange({...e,[k]:ev.target.value})}/></label>)}</div>}</div>}
