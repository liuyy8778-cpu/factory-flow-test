'use client';
import {Select,SelectContent,SelectItem,SelectTrigger,SelectValue} from '@/components/ui/select';
import {DecimalInput} from './editor-tools';
import type {DiameterCondition} from './catalog-filter';
export default function DiameterFilter({value,onChange,label="來料外徑",placeholder="例如 22"}:{value:DiameterCondition;onChange:(v:DiameterCondition)=>void;label?:string;placeholder?:string}){return <div className="catalog-filter-field"><span>{label}（mm）</span><div className="diameter-condition"><Select value={value.op} onValueChange={op=>onChange({...value,op})}><SelectTrigger aria-label={label+"比較方式"}><SelectValue/></SelectTrigger><SelectContent>{[['all','全部'],['eq','= 等於'],['gt','> 大於'],['ge','≥ 大於等於'],['lt','< 小於'],['le','≤ 小於等於'],['range','區間（含兩端）']].map(([v,l])=><SelectItem key={v} value={v}>{l}</SelectItem>)}</SelectContent></Select>{value.op!=='all'&&<DecimalInput aria-label={label+"數值"} placeholder={placeholder} value={value.value} onChange={e=>onChange({...value,value:e.target.value})}/>} {value.op==='range'&&<DecimalInput aria-label={label+"上限"} placeholder="上限" value={value.end} onChange={e=>onChange({...value,end:e.target.value})}/>}</div>{value.op==='range'&&value.value&&value.end&&Number(value.value)>Number(value.end)&&<small className="batch-invalid">下限不可大於上限</small>}</div>}
